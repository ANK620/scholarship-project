__pycache__/
*.pyc
git add .
git commit -m "Initial commit with README and .gitignore"
git branch -M main
git remote add origin https://github.com/ANK620/scholarship-project.git
git branch -M main
git push -u origin main 
git checkout -b my-feature
git add README.txt
git commit -m "Update README with feature"

git checkout main
git merge my-feature
git push
code hello.py
# hello.py
name = input("noeline: ")
print(f"Hello, {noeline}!")

# Simple loop
for i in range(3):
    print("Python is fun!", i)
    git add .
    git commit -m "Initial commit with README and .gitignore"
    git branch -M main
git remote add origin https://github.com/ANK620/scholarship-project.git
git push -u origin main
